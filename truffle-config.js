require('babel-register');
require('babel-polyfill');

//connecting the truffle project to the blockchain_________________

module.exports = {
  networks: {
    development: {
      host: "127.0.0.1",
      port: 7545,
      network_id: "*" // Match any network id
    },
  },

   //____reconfigured to xpose smart contract paths to client as most cases smart contractors arin migration folder
  contracts_directory: './src/contracts/',
  contracts_build_directory: './src/abis/',
  compilers: {
    solc: {
      optimizer: {
        enabled: true,
        runs: 200
      },
      evmVersion: "petersburg"
    }
  }
}
